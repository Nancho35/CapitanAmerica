<%= form_with(model: companero, local: true) do |form| %>
  <% if companero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(companero.errors.count, "error") %> prohibited this companero from being saved:</h2>

      <ul>
      <% companero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="field">
    <%= form.label :especialidad %>
    <%= form.text_field :especialidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
