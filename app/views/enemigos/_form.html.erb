<%= form_with(model: enemigo, local: true) do |form| %>
  <% if enemigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enemigo.errors.count, "error") %> prohibited this enemigo from being saved:</h2>

      <ul>
      <% enemigo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :edad %>
    <%= form.text_field :edad %>
  </div>

  <div class="field">
    <%= form.label :poderes %>
    <%= form.text_area :poderes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
